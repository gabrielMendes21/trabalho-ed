#ifndef CADASTRAR_PROFESSORES_H
#define CADASTRAR_PROFESSORES_H

#include <stdbool.h>

#include "structs/professor.h"

bool cadastrar_professores(Professor profs[], int *qtde_profs)
{
    // Ler o arquivo `professor.txt` e criar um dado do tipo `Professor` para cada um
    // Não se esqueca de atualizar a quantidade de professores no vetor
    return true;
}

#endif