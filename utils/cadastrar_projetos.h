#ifndef CADASTRAR_PROJETOS_H
#define CADASTRAR_PROJETOS_H

#include <stdbool.h>

#include "structs/TG.h"

bool cadastrar_projetos(TG projetos[], char cursos[][], int *qtde_projetos)
{
    // Passar por todos os arquivos `PI_XXX.txt` (Substitua `XXX` por cada sigla do vetor `cursos[][]`)
    // criar um projeto integrador do tipo `TG` e adicionar ele ao vetor `projetos[]`
    // Não se esqueca de atualizar a quantidade de projetos no vetor
    return true;
}

#endif